---
typora-root-url: ../imgs/5.具身
typora-copy-images-to: ../imgs/5.具身
---

[TOC]

# 1. 仿真：把不可重复的失败变成可控实验

## 1.1 概览

仿真用于验证控制接口、生成示范、训练策略和反复回归。要让结果可迁移，首先要明确仿真中的机器人、观测和动作，与真机分别对应什么。资料核对日期：2026-09-21。

![仿真到真机：运动学、动力学、视觉延迟、真机验证及参数校准闭环](/sim-to-real.png)

图为 AI 辅助教学示意。建议沿图从左到右调试：先确认动作接口，再处理接触、视觉与延迟。域随机化应该围绕测得的现实范围展开。

# 2. 工具所处的层次

| 工具 | 主要定位 | 读文档时关注 |
| --- | --- | --- |
| MuJoCo | 关节系统与接触动力学引擎 | 模型、执行器、时间步长 |
| Isaac Sim | 机器人场景、传感器与仿真平台 | 资产、渲染、接口 |
| Isaac Lab | 基于 Isaac Sim 的机器人学习框架 | 环境配置、训练与随机化 |
| ManiSkill | 机器人操作环境与学习工具 | 控制模式、任务、并行采样 |
| LIBERO | 面向机器人终身学习的任务基准 | 任务套件、划分、成功条件 |

MuJoCo 的引擎定位见 [官方概览](https://mujoco.readthedocs.io/en/stable/overview.html)；Isaac Lab 与底层平台的关系见 [Isaac Lab 文档](https://isaac-sim.github.io/IsaacLab/main/index.html)；操作环境见 [ManiSkill 文档](https://maniskill.readthedocs.io/en/latest/)；基准定义见 [LIBERO 仓库](https://github.com/Lifelong-Robot-Learning/LIBERO)。

<font color='#d33682'>物理引擎、训练框架和评测基准不在同一层。</font>采用某个引擎，并不代表自动采用同一套任务或评测协议。

# 3. 构建一个抓放环境

## 3.1 场景与资产

场景包含机器人、夹爪、桌面、杯子、托盘和相机。为每个资产记录几何、碰撞体、质量、惯量及接触参数。渲染网格很好看，不代表碰撞网格适合抓取；过于粗糙的碰撞体可能让夹爪在视觉接触前就停住。

## 3.2 三个时间尺度

- 物理步长：求解动力学的时间间隔。
- 控制周期：控制器更新目标的间隔。
- 策略周期：模型读取观测并生成新动作的间隔。

教学算例：物理步长 2 ms，每 10 个物理步更新控制，则控制频率为 50 Hz；策略若每 5 个控制步更新，则为 10 Hz。三个频率不能仅用一个 fps 描述。

## 3.3 动作接口

先选择关节位置、关节速度、力矩或末端位姿等控制模式，再定义模型输出。若使用末端目标，还需要逆运动学或运动控制器把目标变成关节命令。控制模式改变后，即使动作数组维度不变，原策略也不一定兼容。

# 4. 从确定性调试到域随机化

## 4.1 先做固定场景

1. 固定初始姿态，手动验证各关节运动方向。
2. 发送一个小位移目标，检查坐标与尺度。
3. 做最简单的接近、夹紧、抬起和放下。
4. 保存种子、环境版本、资产和回放轨迹。
5. 再引入视觉策略与场景变化。

固定种子有助于对照，但并不保证不同 GPU、引擎版本或并行配置逐位一致。

## 4.2 域随机化的四组参数

| 随机化组 | 示例 | 过度随机化的风险 |
| --- | --- | --- |
| 外观 | 纹理、光照、背景 | 破坏依赖颜色的任务标签 |
| 几何 | 物体初始位姿、相机位置 | 目标不可见或不可达 |
| 动力学 | 摩擦、质量、阻尼 | 产生现实不存在的极端行为 |
| 时序 | 控制延迟、丢帧、噪声 | 模型只学会保守等待 |

域随机化的基本思路可参考 [Domain Randomization 论文](https://arxiv.org/abs/1703.06907)。本表是实验设计建议；具体分布应依据设备测量和目标场景确定。

# 5. Sim-to-Real 的误差来源

## 5.1 感知差距

材质、曝光、运动模糊、镜头畸变与遮挡会改变图像。先比较同一场景的真机与仿真画面，检查几何关系，再考虑视觉增强。

## 5.2 动力学差距

接触摩擦、夹爪顺应性、传动间隙与物体变形可能主导任务。模型在仿真中依赖不现实的“强摩擦抓取”，真机上就容易滑落。布料、液体和软物体尤其需要单独验证。

## 5.3 时序差距

真机图像可能已过时，网络推理又增加延迟。不能在仿真里用实时状态训练，却在部署时不计延迟地使用旧画面。记录采集到命令执行的完整时间链路。

# 6. 评测协议

成功条件要能操作化。例如“杯子放进托盘”应明确杯子位于指定区域、夹爪已释放、保持稳定一段时间，并且未倾倒。阈值由任务协议给出，不依赖观看者主观判断。

建议报告：每个种子的结果、总成功次数、任务时间、失败阶段、重抓次数，以及是否使用额外特权状态。若训练能看到物体真值位姿，而部署只有 RGB，需说明这种信息差如何处理。

# 7. 排错速查

- 仿真也无法完成：先查接口、奖励和控制器，不急于归因模型容量。
- 仿真成功、真机方向错误：先查坐标与关节顺序。
- 真机运动正确但抓不稳：查接触参数、夹爪力与目标误差。
- 初始抓取成功、长任务失败：查闭环周期、误差累积与恢复行为。

将失败变成可重复的初始状态，再一次只改变一组参数，通常比无差别扩大随机化范围更容易找到原因。


# 扩展图解：接口、机制与实验

## 1. 物理步与策略步

![物理步与策略步](/sim-clock.png)

物理积分步长与策略控制周期通常不同。若每四个物理步更新一次动作，策略频率是物理频率的四分之一；中间控制命令如何保持或插值必须明确。

图中控制降频影响稳定性、延迟与可学习动作尺度。改变物理步长后，若不调整控制周期、奖励累计和终止时间，实验已不再是同一任务。

## 2. 接触与参数辨识

![接触与参数辨识](/sim-contact.png)

接触任务对摩擦、质量、形状、执行器增益和延迟敏感。外观相同的物体可能有不同质心和表面特性，因此逼真的渲染不能替代动力学校准。

图中参数辨识用真实轨迹约束仿真响应。应保留独立验证动作，避免只把某条轨迹拟合得很好；若多个参数组合都能解释观测，应承认辨识不唯一，并测试策略对这些组合的鲁棒性。

## 3. 域随机化的边界

![域随机化的边界](/sim-randomize.png)

域随机化在训练中改变外观、动力学和传感器条件，使策略不依赖过窄环境。随机范围过小可能覆盖不足，过大则产生不现实任务并浪费学习容量。

图中的参数不是任意抖动：应基于测量、制造公差或真实变化设定分布。逐类消融视觉与动力学随机化，才能知道收益来自哪里。[域随机化原论文](https://arxiv.org/abs/1703.06907)。

## 4. 仿真到真机的证据链

![仿真到真机的证据链](/sim-transfer.png)

迁移先验证接口与动作回放，再做仿真闭环，最后在可控真实场景评测。各阶段使用一致任务定义、初始条件范围与成功标准，才能定位差距。

图中的失败反馈用于区分感知偏移、动力学偏移和执行延迟。报告真实试验次数、干预与超时，不把仿真成功率当作真机成功率；也不应只挑选最接近训练分布的演示。
