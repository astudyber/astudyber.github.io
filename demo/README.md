# demo 🎨

这里用于存放除个人主页之外的其他静态前端展示页，例如交互实验、作品集、数据可视化和组件示例。

建议每个展示页使用独立子目录，并以 `index.html` 作为入口：

```text
demo/
└── project-name/
    ├── index.html
    ├── assets/
    └── README.md
```

部署后可通过 `https://astudyber.github.io/demo/project-name/` 访问。
